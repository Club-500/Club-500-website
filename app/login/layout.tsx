export const metadata = {
  title: "Sign in | Club500",
  description: "Sign in to your Club500 fan dashboard, predictions and earnings.",
};

export default function SectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}

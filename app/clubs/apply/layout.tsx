export const metadata = {
  title: "Apply to join Club500 | Club500",
  description: "Register your community football club for onboarding to the Club500 national platform.",
};

export default function SectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}

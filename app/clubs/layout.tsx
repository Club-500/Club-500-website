export const metadata = {
  title: "Find your club | Club500",
  description: "Browse Club500 community football clubs across Kenya's 47 counties and follow yours.",
};

export default function SectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}

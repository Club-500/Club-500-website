export const metadata = {
  title: "Events | Club500",
  description: "The Club500 National Launch Tour, workshops and community club events.",
};

export default function SectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}

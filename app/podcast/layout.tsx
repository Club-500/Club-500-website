export const metadata = {
  title: "Podcast | Club500",
  description: "Stories, strategy and transformation from Kenya's football future.",
};

export default function SectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}

export const metadata = {
  title: "Newsroom | Club500",
  description: "Club and movement news from every county, reported by accredited local journalists.",
};

export default function SectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}

export const metadata = {
  title: "Why Club500 exists | Club500",
  description: "The vision, mission and model behind Kenya's grassroots football movement.",
};

export default function SectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}

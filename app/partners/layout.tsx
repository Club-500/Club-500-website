export const metadata = {
  title: "Partner with Club500 | Club500",
  description: "Back 500 clubs and reach a nation. National, regional, county and pillar sponsorships.",
};

export default function SectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}

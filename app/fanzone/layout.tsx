export const metadata = {
  title: "Fan Zone | Club500",
  description: "Predictions, MVP votes, leaderboards and referral earnings for Club500 fans.",
};

export default function SectionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
